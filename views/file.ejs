<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="/css/binder.css" />
    <link rel="stylesheet" href="/css/file.css" />
    <script
      src="https://kit.fontawesome.com/81e5ddd343.js"
      crossorigin="anonymous"
    ></script>
    <title>File | FileUploader</title>
  </head>
  <body>
    <%- include('partials/nav') %>
    <main>
      <div class="binder">
        <% if (data && data.folderId) { %>
        <a href="/binder/folder/<%= data.folderId %>" class="binder-link">
          <i class="fa-solid fa-arrow-left"></i> My Folder
        </a>
        <% } else { %>
        <a href="/binder" class="binder-link">
          <i class="fa-solid fa-arrow-left"></i> My Binder
        </a>
        <% } %>
        <div class="file">
          <file-data>
            <h1 class="name"><%= data.name %></h1>
            <div class="size"><strong>Size:</strong> <%= data.size %></div>
            <div class="upload">
              <strong>Uploaded At:</strong> <%= data.uploadedAt %>
            </div>
            <div class="update">
              <strong>Updated At:</strong> <%= data.uploadedAt %>
            </div>
          </file-data>
          <div class="download">
            <button class="download-btn">
              <a href="/binder/file/download/<%= data.id %>">Downlaod</a>
            </button>
          </div>
        </div>
      </div>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
